# Statistic Harness

Local-only, plugin-first statistical analysis harness.

## Quickstart

```bash
python -m venv .venv
. .venv/bin/activate
pip install -e ".[dev]"
stat-harness list-plugins
```

## Development

Run tests:

```bash
python -m pytest -q
```

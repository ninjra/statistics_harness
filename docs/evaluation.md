# Evaluation Harness

The evaluator compares `report.json` against a ground truth YAML file to assert
expected findings (feature discovery, changepoints, dependence shifts, and
anomalies) within configured tolerances.
